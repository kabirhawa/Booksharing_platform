import { Box, CircularProgress } from "@mui/material";
import React from "react";

const PageNotFound = () => {
  return (
    <Box sx={{margin:'auto'}}>
      <CircularProgress />
    </Box>
  );
};

export default PageNotFound;
